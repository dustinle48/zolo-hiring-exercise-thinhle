/* 
//should also use testing-library/react

describe("Offer tab", {} => {
    it("Only employee associates with the offer could view it",() => {
        //test case
    })

    it("Re-fetching API should be called periodically",() => {
        //test case
    })

    it("Re-fetching API actually update new comments",() => {
        //test case
    })
}) 
*/
